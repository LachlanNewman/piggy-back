# OIDC provider base URL. For Auth0 this is https://<tenant>.<region>.auth0.com/
VITE_OIDC_AUTHORITY=

# Client ID registered with your OIDC provider
VITE_OIDC_CLIENT_ID=

# Redirect URI — must match exactly what is registered with your OIDC provider
VITE_OIDC_REDIRECT_URI=http://localhost:5173/callback

# Identifier of the API registered with the provider. Auth0 issues an opaque
# access token unless an audience is requested, and an opaque token cannot be
# verified by the backend — so this must match OIDC_AUDIENCE on the backend.
VITE_OIDC_AUDIENCE=

# Base URL of the backend API. Leave empty for local dev — Vite proxies /api to
# the backend. Set to the backend's public URL when the frontend is deployed
# separately (e.g. https://piggy-back-97mj.onrender.com). Read at build time.
VITE_API_BASE_URL=
